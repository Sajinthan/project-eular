// type BigInt = number;
// declare const BigInt: typeof Number;
function findPermutedMultiples() {
    for (var i = 0; i < 10; i++) {
        var num = Number('3' + i.toString());
        console.log(num);
        console.log(num * 2);
    }
}
