// type BigInt = number;
// declare const BigInt: typeof Number;
function findPermutedMultiples() {
    for (let i = 0; i < 10; i++) {
        const num = Number('3' + i.toString());
        console.log(num);
        console.log(num * 2);
    }
}
